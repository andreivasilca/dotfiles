# Claude / agent guidance for this repo

Personal dotfiles repo. macOS terminal-first setup: Neovim (kickstart base), Ghostty, a minimal vim safety net. Managed with GNU stow.

## Agent skills

### Issue tracker

GitHub Issues on `andreivasilca/dotfiles`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `backlog`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo. `CONTEXT.md` at root, ADRs under `docs/adr/`. See `docs/agents/domain.md`.
